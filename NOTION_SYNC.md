# Notion 연동 인수인계 (2026-08-08 갱신)

> **구분:** 이 문서는 **수집 데이터 DB**(계정·체인·게시물·템플릿·심리기법)용이다. 제품 기획 IA(PRD·Story·Features·…)의 SSOT는 `.omd/dbs` 로컬 HTML이다.

새 세션에서 이 문서를 읽고 이어서 진행할 것.
**이전 버전(2026-08-01)의 §4-(1) 진단은 틀렸다.** 아래 §5에 정정 내용이 있다.

> **2026-08-08: 수집 방식이 통째로 바뀌었다.** DOM 스크래핑을 버리고 SSR JSON 파싱으로 갔다.
> 조회수를 처음 확보했고 초반 유지율이 처음 측정됐다. 그 과정에서 **기존 체인 데이터의 80%가
> 틀렸다는 것이 확인됐다.** 반드시 [shared/crawler/README.md](shared/crawler/README.md) 를 먼저 읽을 것.
> 아래 §5 의 (1)·(2)·(5) 는 DOM 시절 이야기이며 지금은 해당하지 않는다.

---

## 1. 목표

| 유튜브 | 스레드 | 측정 지표 |
|---|---|---|
| 썸네일 | **1번째 글 (훅)** | 클릭률 |
| 초반 30초 | **2번째 글** | 초반 이탈률 |
| 중반 기획 | **3번째 글 이후** | 중반 이탈률 |

최종 목적은 **터진 콘텐츠 타입을 찾아 템플릿화**하는 것.
저작권 문제 때문에 남의 표현을 그대로 쓰지 않고, **심리 기법으로 분해해 우리 문장으로 재조립**한다.

---

## 2. Notion 구조 (전부 Holiday Investment `3aa346dac456801ab7cde078793bfa83` 하위)

| DB | data_source_id | 상태 |
|---|---|---|
| 스레드 계정 | `3af346da-c456-800c-9eb4-000b97ed7d58` | 2행 |
| 스레드 체인 | `8b9eb3e5-e240-4dff-87b1-43cfde89ee69` | **866행** (+ 중복 200행 삭제 대기) |
| 스레드 게시물 | `0f9851e7-c9f0-45bc-909b-6aef4064c9dd` | **1,422행 완료** |
| 스레드 템플릿 | `4e6dc325-c5a6-4132-a750-71377ef3cd09` | **28행** (추출 18 + 자체제작 10) |
| 심리 기법 | `fc5ef4ac-3054-476d-ba69-34ad5c0d9991` | **17행** |

계정 행: `beyond.cho` = `3af346dac456813cb549e5cd33a1fba8` · `ho_ho_ju_ju` = `3af346dac456810f9821c0b06824a480`

관계 배선: 게시물↔체인, 게시물↔계정, 체인↔템플릿, 템플릿↔심리기법, 템플릿↔계정 (전부 양방향)

---

## 3. ⚠️ 즉시 해야 할 사용자 작업

**스레드 체인 DB에 중복 200행이 있다.** 아카이브가 이 환경의 권한 분류기에 막혀 삭제하지 못했다.

- `주제` = `⚠️ 중복 - 삭제 대상` 으로 표시돼 있다
- Notion UI에서 그 값으로 필터 → 전체 선택 → 삭제하면 866행으로 복원된다
- 감사 기록: `data_export/duplicate_chain_pages.json`
- 이 200행은 전부 손상된 텍스트도 함께 갖고 있다 (아래 §5-(3) 참조). 정상 866행은 무결하다

---

## 4. 적재 방법 — **반드시 스크립트로**

`shared/load_posts_to_notion.py`, `shared/load_psych_to_notion.py`

```bash
python3 shared/load_posts_to_notion.py --dry-run   # 확인
python3 shared/load_posts_to_notion.py             # 적재
```

**MCP 툴콜로 대량 한국어를 넣지 말 것.** 이 세션에서 세 번 시도했고 세 번 다 한국어가 깨졌다
(`골드만삭스`→`계율삭스`, `뭐가`→`뻐가`, `나눴습니다`→`나똌습니다`).
배치 크기·이스케이프 방식·검증 주기를 전부 바꿔봤지만 원인은 하나였다 — **모델이 텍스트를 재생성하는 것.**

해법은 모델을 데이터 경로에서 빼는 것이다. 스크립트가 JSON을 읽어 `ntn api v1/pages` 에
stdin 으로 넘긴다. 1,162건이 실패 0으로 한 번에 끝났다.

**`ntn` 은 Notion 공식 CLI이며 이미 설치·인증돼 있다** (워크스페이스 Pax Humana).
별도 토큰 발급이 필요 없다. `ntn doctor` 로 상태 확인.
버전이 낮으면(`v0.14.0`) `ntn update`.

---

## 5. 이전 문서의 오진단 정정

**(1) `2 / 11` 은 답글 페이지네이션이 아니라 체인 순번 배지다.**
Threads 가 이 배지를 **본문 span 안에 중첩**시켜서 크롤러가 끌고 들어온 것.
즉 오염된 CSV 행은 사실 순번 정보를 갖고 있었고, 복구해서 썼다.
정제 검증 결과 227행 전부 좋아요·답글이 CSV 원본과 100% 일치.

**(2) 프로필 피드는 forward-only 가상화다.**
지나간 카드는 영구 언마운트되고 **위로 스크롤해도 복구되지 않는다** (130,000px·3초 대기로 검증).
수동 스크롤 후 일괄 수확은 성립하지 않는다. 위→아래로 내려가며 증분 수확해야 한다.
(당시 로직은 `shared/extract_chain.js`·`harvest_runner.js` 에 있었으나 2026-08-09 삭제했다.
SSR 방식으로 전환돼 더는 쓰지 않는다 — ADR-004.)

**(3) 한국어 손상은 모델이 텍스트를 재생성할 때 발생한다.** §4 참조.

**(4) `javascript_tool` 은 45초에 CDP 타임아웃.** 단, 타임아웃은 응답만 끊고 페이지 안 async 루프는 계속 돈다.

**(5) 조회수는 공개된다.** README 의 "소유자만 조회" 메모는 부정확하다.
글마다 자기 퍼머링크에서 노출되며 (`조회 13만회`), 2 유효숫자 반올림이다.
프로필 목록에는 안 나온다. 체인 헤더 값은 1번 글의 조회수다.

**(6) Notion 은 날짜를 분 단위로 저장한다** (원본은 초 단위). 12쌍이 같은 분에 겹친다.
체인 매핑 키는 **날짜(분) + 훅 앞 30자** — 866개 전부 고유함을 확인했다.
또한 KST/UTC 9시간 오프셋이 있다.

---

## 6. ⛔ Threads 소프트 차단 (2026-08-01 발생, 해제 여부 미확인)

자동 스크롤이 과해서 페이지네이션 GraphQL 응답이 전부
`페이지를 찾을 수 없습니다 • Instagram` HTML 에러 문서로 돌아온다 (39건 연속 확인).

재개 시 지켜야 할 것:
- **한 계정씩만.** 두 계정 병렬이 rate 를 두 배로 만든 결정타였다
- 배치마다 3~8초 + 지터. 사람 속도로
- **에러 응답 감지 + 지수 백오프.** 기존 러너에는 이 감지가 없어 벽에 대고 계속 스크롤했다
- 차단 우회(신원 변경 등)는 하지 않는다

---

## 7. 데이터 현황 (2026-08-08 재수집 완료)

SSR 방식으로 두 계정 전량 재수집했다. **총 1,506 요청, 실패 0건, 차단 0건.**

| | 값 |
|---|---|
| 체인 | **815** (기존 866 중 35쌍이 실은 같은 체인이었다) |
| 글 | **7,819** (기존 1,422 → 5.5배. 기존이 체인을 잘라먹고 있었다) |
| 훅 조회수 | 814 / 815 |
| 2번 글 조회수 | 641 (단일 글 체인 173개는 해당 없음) |
| 누적 훅 조회수 | 46,176,968 |

**기존 `data_export/chains.json` 은 신뢰할 수 없다.** 재구축 결과 80%가 불일치했다.

```
과소 묶음 (기존이 체인을 잘라먹음)  71.5%   ← 대부분이 이것
일치                              19.6%
과대 묶음 (무관한 글을 합침)         7.0%
길이 같지만 멤버 다름                1.6%
```

새 산출물은 `data_export/normalized/` 에 있다 — `posts.json` · `chains.json` ·
`metrics.jsonl`(관측 시계열) · `retention.json`. 원본은 `data_export/raw/*.jsonl`.

> **2026-08-09 정정.** 구 CSV(06-12 스냅샷)는 `accounts/` 삭제와 함께 백업 tar 로 빠졌다.
> `metrics.jsonl` 의 관측 시점은 **08-08 하나뿐**이므로, 현재 스토어만으로는 성장 추이를
> 계산할 수 없다. 같은 글이 7주 만에 2.5배 성장한 사례를 다시 쓰려면
> `~/projects/thread-rocket_cleanup-backup_2026-08-09.tar.gz` 에서 CSV 를 꺼내
> `metrics.jsonl` 로 흡수시켜야 한다.

### 처음 측정된 지표 — 초반 유지율 (2번 글 조회수 ÷ 훅 조회수)

641체인 중앙값 **14.7%**. 훅 유형별로 갈린다.

| 훅 유형 | n | 유지율 | 훅 조회 중앙 | 체인 길이 |
|---|---|---|---|---|
| 시사형 (🚨·ㄷㄷ) | 196 | 18.0% | 46,436 | 13 |
| 목록형 (TOP N·N가지) | 134 | 12.0% | 21,495 | 16 |
| 기타 (훅 장치 없음) | 154 | 9.0% | 16,922 | 4 |

- 시사형이 **도달과 유지 둘 다** 앞선다. 트레이드오프가 아니다.
- 목록형이 낮은 건 구조적이다 — 훅 본문에 목록 앞부분이 이미 들어있어 2번으로 넘어갈 유인이 약하다.
- 체인 길이 ↔ 유지율 순위상관 **+0.274**. 기존 데이터로는 체인이 잘려 있어 관측 불가능했던 관계다.
- 훅 조회수 ↔ 유지율은 **-0.248** — 약한 음의 상관. 별개의 축으로 봐야 한다.

계정 비교: 유지율은 거의 같고(14.7% vs 14.8%), 도달과 참여율이 반대다.

| 계정 | 유지율 | 훅 조회 중앙 | 참여율(좋아요÷조회) |
|---|---|---|---|
| beyond.cho | 14.7% | 25,854 | 0.96% |
| ho_ho_ju_ju | 14.8% | 6,943 | **1.69%** |

전부 상관이며 인과가 아니다. 표본은 계정 2개뿐이다.

---

## 8. 남은 작업

**먼저 결정이 필요한 것 — Notion 재적재.** 현재 Notion 의 스레드 체인 866행·게시물 1,422행은
전부 구 데이터 기준이라 80%가 틀린 체인 구성 위에 서 있다. 재적재 범위를 정해야 한다
(전량 교체 vs 체인 DB 만 교체 vs 신규 DB 병행).

1. ~~중복 200행 삭제~~ · ~~ho_ho_ju_ju 재수집~~ · ~~조회수 수집 → 초반 유지율~~ → **2026-08-08 완료**
2. **템플릿↔체인 재매핑.** 기존 284/866 매핑은 구 chain_id 기준이라 재구축 후 무효다.
   새 `normalized/chains.json` 위에서 다시 분류해야 한다
3. **마지막 글 조회수 → 완주율.** 현재는 1번·2번만 있다. 체인당 1회 더 로드하면
   `완주율 = 마지막 ÷ 1번` 이 나온다 (약 640 요청, 1.5시간)
4. **N1~N10 발행 → 우리 계정 기준선 확보.** 현재 배수는 전부 남의 계정 기준이다.
   우리 계정은 **공식 Threads API** 를 쓰는 게 맞다 — 본인 계정 인사이트는 API 가 정확히 준다
   (남의 계정 인사이트는 API 로 불가능. 그래서 남의 계정만 SSR 수집을 쓴다)
5. **신규 게시물 추적.** SSR 방식은 이미 코드를 아는 글만 받는다.
   신규 글 발견에는 프로필 피드가 필요하고 그건 로그인 세션을 요구한다 (README 참조)

---

## 9. 관련 파일

- [templates/](templates/README.md) — 훅 템플릿 카드 세트 (2026-08-08 재수집 기준). `content_plans/` 의 구 템플릿 문서를 대체한다
- [.claude/skills/thread-hook-analysis](.claude/skills/thread-hook-analysis/SKILL.md) — 훅 분해·태깅 스킬. `scripts/hooks.py` · `scripts/relperf.py` 포함
- **[shared/crawler/README.md](shared/crawler/README.md) — 현행 수집기. 재개 전 필독**
  - `ssr.mjs`(파서) · `fetch.mjs`(수집) · `normalize.mjs`(정규화) · `retention.mjs`(유지율)
- ~~`shared/extract_chain.js` · `shared/harvest_runner.js`~~ **2026-08-09 삭제**
  — DOM 시절 수확기. 2026-08-08 이후 사용하지 않는다 (반올림된 카운트·조회수 없음·체인 오분류)
- [shared/load_posts_to_notion.py](shared/load_posts_to_notion.py) · [shared/load_psych_to_notion.py](shared/load_psych_to_notion.py) — 적재
- ~~`accounts/`~~ · ~~`content_plans/`~~ · ~~`raw_content/`~~ · ~~`archive/`~~ — **2026-08-09 삭제.**
  전량이 `~/projects/thread-rocket_cleanup-backup_2026-08-09.tar.gz` (109개 파일)에 보존돼 있다.
  2026-06-12 CSV 스냅샷도 그 안에만 있으므로, 성장 추이를 다시 보려면 압축을 풀어야 한다
