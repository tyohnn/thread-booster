.env.local
node_modules/
shared/crawler/.profile/
.DS_Store

# 수집 원문 — 제3자 게시물 전문이므로 공개 저장소에 올리지 않는다 (POL-002)
# 재생성: shared/crawler 의 fetch → normalize 파이프라인
data_export/

# 설치형 스킬 팩 — skills-lock.json 으로 복원한다 (npx skills add)
# .claude/skills 아래 팩 항목은 .agents/ 를 가리키는 심링크라 그대로 커밋하면 깨진다
.agents/
.claude/skills/*
!.claude/skills/thread-hook-analysis/
